# Local dev only. Copy to .env (gitignored) for `adk run` / `adk web`.
# In Cloud Run these are injected from Secret Manager (see infra/gcloud_deploy.sh).
GOOGLE_GENAI_USE_VERTEXAI=FALSE
GOOGLE_API_KEY=your-google-ai-studio-key
